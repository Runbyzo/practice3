; copy_array_fixed.asm
; Копирование массива из адреса 200 в адрес 300

; 1. Инициализируем исходный массив
CONST 1
CONST 200
STORE           ; MEM[200] = 1

CONST 2
CONST 201
STORE           ; MEM[201] = 2

CONST 3
CONST 202
STORE           ; MEM[202] = 3

; 2. Копируем элемент из 200 в 300 (простой тест)
CONST 200       ; загружаем адрес 200 на стек
LOAD 200        ; читаем MEM[200] (должно быть 1) на стек
CONST 300       ; загружаем адрес 300 на стек
STORE           ; записываем 1 в MEM[300]

; 3. Проверяем копирование
CONST 300       ; загружаем адрес 300
LOAD 300        ; читаем MEM[300]
CONST 1         ; ожидаемое значение 1
!=              ; сравниваем
